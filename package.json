{
  "name": "jekyll-up",
  "version": "2.0.1",
  "description": "Get up and running with a minimal Jekyll site scaffold",
  "scripts": {
    "build": "bundle exec jekyll build",
    "deploy": "npm run build && gh-pages -d _site",
    "start": "bundle exec jekyll serve",
    "test": "standard assets/main.js"
  },
  "author": "Advait Chaitanya",
  "private": true,
  "license": "MIT",
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "standard": "^17.1.2"
  }
}
